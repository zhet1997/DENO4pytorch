---
alwaysApply: true
---
# Research Code Rules.mdc

**版本**：1.0
**适用范围**：本仓库（科研个人使用）
**目标**：以“**简洁、可读、可调试**”为第一优先，减少过度工程化与难以维护的复杂性。

---

## 0. 核心原则

1. **简洁优先**：能用简单直白的实现解决问题时，不引入额外抽象与通用层。
2. **可调试优先**：遇错能迅速定位根因；拒绝吞错、拒绝隐式副作用。
3. **近期有效优先**：以当前需求为准，默认不做向后兼容。

---

## 1. 控制分支复杂度（避免“大网兜 if-else”）

* **要求**

  * 不编写“面面俱到”的大范围情况覆盖；**每个函数只处理其职责范围内的输入**。
  * **单个函数的条件分支建议 ≤ 3 层**；遇到分支爆炸，**拆函数**或**表驱动**（映射表/策略字典）。
* **禁止**

  * 在同一函数内堆叠大量 `if-elif-else` 以覆盖“可能会遇到”的远端情况。
* **建议替代**

  * **早返回**（guard clauses）、**策略映射**、**小函数拆解**。

**示例（反例 → 正例）**

```python
# 反例：大网兜 if-else
def render(val, mode, fmt, lang, theme, compact):
    if mode == "a":
        if fmt == "json":
            ...
        elif fmt == "csv":
            ...
    elif mode == "b":
        ...
    # 还在往下堆……

# 正例：表驱动/小函数
RENDERERS = {
    ("a", "json"): render_a_json,
    ("a", "csv"): render_a_csv,
    ("b", "json"): render_b_json,
}

def render(val, mode, fmt):
    try:
        fn = RENDERERS[(mode, fmt)]
    except KeyError:
        raise ValueError(f"Unsupported mode/fmt: {mode}/{fmt}")
    return fn(val)
```

---

## 2. 异常处理（默认不写 try/except）

* **默认规则**

  * **不要**为一般逻辑写 `try/except`；**让错误抛出**，便于定位问题。
* **仅在以下明确情况允许使用 `try/except`**：

  1. **大批量/批处理生成**（需要跳过少量失败样本继续整体任务）；
  2. **外部程序/系统调用**（如子进程、网络、GPU 驱动）存在**已知概率性失败**；
  3. **资源清理**（确保文件句柄、临时目录、显存释放）；
  4. **极窄范围**的容错，且**必须**在异常中**记录上下文**（输入、命令、参数）并**向上抛/汇总报告**。
* **禁止**

  * 空的或笼统的 `except Exception: pass`；
  * 捕获后仅打印不抛出，导致失败被吞没。

**示例（允许场景）**

```python
# 外部程序：允许
def run_cli(cmd: list[str]) -> str:
    cp = subprocess.run(cmd, capture_output=True, text=True)
    if cp.returncode != 0:
        # 带上下文抛错
        raise RuntimeError(f"cmd failed: {cmd}\nstdout={cp.stdout}\nstderr={cp.stderr}")
    return cp.stdout

# 批处理：允许（记录失败样本）
def process_batch(items):
    results, errors = [], []
    for it in items:
        try:
            results.append(process_one(it))
        except Exception as e:
            errors.append((it, repr(e)))
    if errors:
        print(f"[warn] {len(errors)} items failed")  # 或写入日志/报告
    return results
```

---

## 3. 参数传递（显式优先）

* **要求**

  * 函数入参**显式列出**所需信息；**禁止**把一个“超大数据结构/全局 cfg”当万能输入四处传递。
  * 如需组合配置，使用**专用的小型配置对象**（`dataclass`/`pydantic`），**仅含当前模块所需字段**。
* **禁止**

  * 在函数内部读取全局可变状态（含环境变量）以改变行为。
* **建议**

  * 所有函数**标注类型**与**简明 docstring**；逻辑输入输出可被静态检查工具（如 mypy）理解。

**示例**

```python
@dataclass
class SolverConfig:
    max_iter: int
    tol: float

def solve(A: np.ndarray, b: np.ndarray, cfg: SolverConfig) -> np.ndarray:
    ...
```

---

## 4. 文件体量（单文件 ≤ 300 行）

* **要求**

  * **单个源文件代码行数 ≤ 300 行**（**注释与空行不计**）。
  * 超出则**拆分模块**：类型/模型、核心算法、I/O/可视化、脚手架/CLI 分离。
* **说明**

  * 保持“屏幕一眼看清”的可维护性；新功能优先新模块而非“继续加到旧文件尾部”。

---

## 5. 兼容性与版本保留（默认不做）

* **默认规则**

  * **不考虑向后兼容**；没有明确说明时，**以最新需求覆盖旧行为**。
  * **不保留**历史版本分支/参数开关来“兼容旧接口”。
* **允许的例外**

  * 明确提出“需要兼容 **X** 版本”，并在 MR/提交描述中**写明兼容边界与测试用例**。
* **目的**

  * 避免“每改一次就堆一层 if 兼容旧版”的代码膨胀与行为歧义。

---

## 6. 代码风格与结构

* **函数职责**：**单一职责**，**函数 ≤ 50 行**为宜；必要时拆分为内部私有小函数。
* **日志与可观测性**：在模块边界打印/记录**关键参数、数据形状**；禁止在内部深处静默修改输入。
* **依赖**：未明确说明不得新增第三方库；确需新增时在提交信息中说明用途与可替代方案。
* **命名**：以**含义**为先（`compute_flux`、`areal_power`），避免缩写黑话。
* **注释**：解释“为什么”，而非逐行“怎么做”。



**注**：本规则文件用于个人科研代码管理，旨在提升迭代速度与定位问题的效率；如与具体任务冲突，请先以任务正确性为准，并按“例外处理流程”记录。
